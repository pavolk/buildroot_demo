image boot.vfat {
	vfat {
		files = {
			"boot.bin",
			"uboot.scr",
			"devicetree.dtb",
			"uImage"
		}
		file uramdisk {
			image = "rootfs.cpio.uboot"
		}
	}
	size = 64M
}

image sdcard.img {
	hdimage {
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "boot.vfat"
	}
}
